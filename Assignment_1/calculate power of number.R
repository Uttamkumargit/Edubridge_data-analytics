# Write a program to calculate power of number
x <- as.integer(readline(prompt = "Enter the number: "))
y <- as.integer(readline(prompt = "Enter the power: "))

power_num <- x^y
print(paste("Power of number is: ",power_num))